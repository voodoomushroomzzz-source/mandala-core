name: Build bot_built.py

on:
  push:
    paths:
      - 'honeycombs/fruits/gentle_companion/**.py'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run build.py
        run: python honeycombs/fruits/gentle_companion/build.py

      - name: Syntax check bot_built.py
        run: python -c "import ast; ast.parse(open('bot_built.py',encoding='utf-8').read()); print('Syntax OK')"

      - name: Commit bot_built.py
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add bot_built.py
          git diff --staged --quiet || git commit -m "auto: rebuild bot_built.py [skip ci]"
          git push
